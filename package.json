{
  "name": "cfw-easy-utils",
  "version": "0.1.2",
  "description": "An utils library for common tasks with CloudFlare Workers.",
  "main": "dist/cfw-easy-utils.js",
  "scripts": {
    "inc": "npm version patch --no-git-tag-version && git add package.json && git commit -m \"Auto-increment version.\" && git push",
    "build": "rollup -c && node postBuild.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "workers",
    "cloudflare",
    "cfw",
    "utils"
  ],
  "author": "Cerulean",
  "license": "MIT",
  "devDependencies": {
    "@neriko/cloudflare-workers-router": "^0.1.1",
    "rollup": "^2.38.5",
    "rollup-plugin-inject-process-env": "^1.3.1"
  },
  "dependencies": {
    "cfw-easy-html": "^0.1.9",
    "cookie": "^0.4.1",
    "rollup-plugin-terser": "^7.0.2"
  }
}
